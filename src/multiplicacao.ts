    function multiplicacacao(a: number, b: number): number {
        return a * b;
    }
    
    function valor(nome: string): string {
        return "Olá " + nome;
    }
    
    // Exemplos de uso
    console.log(multiplicacacao(5, 3)); 
    console.log(valor("Vitor")); 
    
